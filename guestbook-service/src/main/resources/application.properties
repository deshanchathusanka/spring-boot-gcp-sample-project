# development environment properties

server.port=${PORT:8081}

# disable cloud sql for development
spring.cloud.gcp.sql.enabled=false

# disable cloud trace for development
spring.cloud.gcp.trace.enabled=false